PORT=3000
# DATA_DIR: set to /data on Render (persistent disk mount); leave unset for local dev
DATA_DIR=
# DB_PATH: overrides DATA_DIR for the database location if set explicitly
DB_PATH=
ANTHROPIC_API_KEY=your_key_here
TANNINS_BAR_PASS=changeme
